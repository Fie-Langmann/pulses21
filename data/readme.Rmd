the "~/data" folder contains data after initial data management. In this folder any future edits of data and subsets of datasets should be stored so that raw data are not altered.
Due to personally identifiable information, "~/data" does not contain data.csv
