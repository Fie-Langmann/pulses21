the "~/data_raw" folder contains a .csv with raw data from the 2021 pulses survey and a .xlsx sheet with raw data and meta data explaining the coding of the raw data contained in the .csv file

The create_data.R script explains how the excel data sheet was converted to a .csv
